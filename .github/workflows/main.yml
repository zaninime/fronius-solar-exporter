name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: cachix/install-nix-action@v6
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: nix-build -A containerImage release.nix
