package me.zanini.froniussolar.metrics

object Constants {
  val GlobalMetricsNamespace = "solar"
  val SiteMetricsNamespace = s"${GlobalMetricsNamespace}_site"
  val InverterMetricsNamespace = s"${GlobalMetricsNamespace}_inverter"
}
